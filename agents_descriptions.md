# Tlamatini — Agent Descriptions

This file is the **authoritative source of human-readable descriptions** for every workflow agent shown in the Agentic Control Panel sidebar of Tlamatini.

The Django view `agent.views.agentic_control_panel` parses the table rows under `## Workflow Agents` below and injects the `Description` column into the page as `agent_purpose_map` JSON. The frontend (`acp-canvas-core.js`) reads that JSON and uses the text as the **hover tooltip** over each sidebar agent and as the **Description** dialog body when the user right-clicks an agent on the canvas.

Editing a row's `Description` cell here changes both:
- the documentation read by humans, AND
- the live UI text displayed to end users.

The lookup is case- and punctuation-insensitive: the key for each row is `re.sub(r'[^a-z0-9]+', '', name.lower())`. So "Kyber-KeyGen", "Kyber KeyGen", and "kyberkeygen" all resolve to the same description.

Descriptions are written for **non-expert readers** — when in doubt, prefer the longer, friendlier phrasing over a terse one-liner. A user who has never seen the agent before should be able to understand **what it does, when to use it, and what its main inputs/outputs are** from a single tooltip read.

---

## Workflow Agents

The visual workflow designer ships with **76 agent types**, grouped below by their role in a flow. Drag any of them onto the canvas, wire connections between them, and press *Start* — the agents run as separate, log-emitting processes that talk to each other through their `config.yaml` files.

### Control Agents

These agents shape the lifecycle of a flow: when it begins, when it ends, and what gets cleaned up afterward.

| Agent | Description |
|-------|-------------|
| **Starter** | The entry point of every flow. When the user clicks *Start*, the Starter is the first agent that runs; its only job is to launch the agents listed in its `target_agents`. Think of it as the green "Play" button of the canvas — it does no real work itself, it simply kicks off the chain. |
| **Ender** | Terminates a flow gracefully. Kills every agent listed in its `target_agents`, then launches anything wired into its `output_agents` (typically a FlowBacker for backups, followed by Cleaners that wipe transient files). Use Ender at the bottom of every flow that should shut down on its own. |
| **Stopper** | A surgical "kill the noisy neighbor" agent. It watches the logs of one or more `source_agents` and, when a configured pattern appears (e.g. `FAILED`, `out of memory`), terminates exactly those source agents — without bringing the whole flow down. Stopper does NOT start downstream agents; it is purely a controlled-shutdown tool. |
| **Cleaner** | The post-mortem janitor. After an Ender or FlowBacker completes, the Cleaner deletes the `.log` and `.pid` files of the agents it was wired to, returning the session pool to a clean slate. It is the only agent that should be wired AFTER Ender. |
| **Sleeper** | A timed pause. Waits for a configured number of milliseconds, then starts everything in `target_agents`. Useful for rate-limiting polling loops, giving an external service time to settle, or building "every N seconds" cycles when paired with a Raiser. |
| **Croner** | A clock-driven trigger. Wakes up at the configured `HH:MM` every day and starts its `target_agents`. Good for nightly backups, scheduled reports, or any "run this every day at 3 AM" flow. Pair it with Gatewayer if you also want event-driven triggers in the same flow. |

### Routing Agents

These agents decide WHERE the flow goes next based on what they observe in upstream logs or user input.

| Agent | Description |
|-------|-------------|
| **Raiser** | An event-driven launcher. Continuously watches the logs of its `source_agents` for a configured `pattern`. When the pattern appears, it fires every agent in `target_agents`. Raiser is the bread-and-butter bridge between *passive* monitoring agents and *active* action agents — anywhere you want "when X happens, do Y", a Raiser is the right answer. |
| **Forker** | An automatic A/B router. Watches its source agents for two distinct sets of patterns (`pattern_a` and `pattern_b`); whichever set matches first determines which path the flow takes next (`target_agents_a` or `target_agents_b`). Use it whenever a single upstream signal can mean two different things and each meaning has its own downstream chain. |
| **Asker** | An interactive A/B chooser. When it runs, it pops a dialog in the chat UI asking the user to pick Path A or Path B (with optional captions like "Approve" / "Reject"). The chosen path's `target_agents` are launched and the rejected path is left untouched. Has a 5-minute timeout. Perfect for human-in-the-loop approvals or a/b experiments. |
| **Counter** | A persistent tally with threshold-based routing. Each time it runs it increments a stored counter and compares it against a `threshold_value`. If the counter is below the threshold it fires `target_agents_l` (less); if it has reached or passed the threshold it fires `target_agents_g` (greater). The count survives flow restarts via reanim files, so it can drive "do this 5 times then do that" loops. |

### Logic Gates

These agents hold the flow until specific upstream conditions are met — they are pure synchronization primitives.

| Agent | Description |
|-------|-------------|
| **AND** | A two-input AND gate. Fires its `target_agents` only when BOTH `source_1` AND `source_2` have produced their respective patterns. The match is *latched*: once a source matches, that side stays satisfied until the gate fires. Use it to require two parallel branches to both succeed before continuing. |
| **OR** | A two-input OR gate. Fires its `target_agents` when EITHER `source_1` OR `source_2` produces its pattern, whichever happens first. Use it for "the first one wins" patterns, e.g. continue as soon as either of two redundant data sources finishes. |
| **Barrier** | An N-input barrier (a generalized AND). Waits until ALL configured `source_agents` have started, then fires its `target_agents`. The first source to arrive becomes the polling watcher; every subsequent arrival drops a flag file and the gate releases when all flags are present. Designed for fan-in synchronization across many parallel branches without any race conditions. |

### Action Agents

These are the agents that *do real work* — running commands, hitting APIs, producing files, calling LLMs, automating the desktop.

| Agent | Description |
|-------|-------------|
| **Executer** | Runs an arbitrary shell command and triggers its `target_agents` when the command finishes. The command's stdout and stderr are captured to its log file. The simplest, most flexible "do something on this machine" agent — anywhere you would have typed a command into a terminal, an Executer can do it for you. **Preferred over Keyboarder / Mouser whenever a command or build step needs to run; never type a command into a terminal window through Keyboarder — give it to Executer instead.** |
| **Pythonxer** | Runs an inline Python script (provided as a YAML string) behind a STRICT correctness gate: it first `compile()`-parses the script (an unparsable script is refused, never run) and then validates it with Ruff — when `ruff_blocking` is true (the default) any Ruff finding aborts execution with the findings logged. The script can use any installed package. **Downstream agents are ALWAYS triggered afterwards — no matter what (success, a gate refusal, or a runtime failure)** — so Pythonxer never dead-ends a flow; the exit code (0/non-zero) drives only the LED and the Multi-Turn fix-and-retry loop. Wire a downstream agent to inspect the result/log if a step must only proceed on success. An optional `execute_forked_window` flag opens the script in a new console for live stdout. Great for ad-hoc data wrangling, custom logic, or anything you'd write a one-off `.py` file for. **Preferred over Keyboarder / Mouser for code execution and computed-content generation; never drive Keyboarder to type Python into IDLE or an editor — feed the code to Pythonxer instead.** |
| **Sqler** | Executes a Python block against a Microsoft SQL Server instance using `pyodbc`. The script receives `cursor` and `conn` as injected globals so you can write `cursor.execute("SELECT ...")` directly. Triggers downstream agents when the SQL completes successfully. Use it for ETL steps, schema checks, data exports, or anything that needs a real SQL session. |
| **Mongoxer** | Executes a Python block against a MongoDB database using `pymongo`. The script receives a pre-connected `db` object so you can write `db.users.find({...})` directly. Triggers downstream agents on success. The MongoDB twin of Sqler — perfect for document-store reads, writes, and aggregations inside a flow. |
| **Crawler** | A developer-oriented web crawler with built-in LLM analysis. Fetches a URL via HTTP GET and captures **raw content by default** (full HTML, inline JS, CSS, headers, JSON-LD, data-* attributes, the lot) or plain text on demand. Optionally walks all links on the same domain or every link found. The result is sent to an Ollama model with a developer-centric system prompt that produces a structured analysis. Outstanding for security audits, competitive analysis, or any "tell me what this page actually does" task. |
| **Googler** | Performs a Google search for a configured query using a real headless browser (Playwright), opens the top N result pages, extracts their readable text, and writes the lot to an output file plus its log. Where Crawler hits a single URL you already know, Googler is the agent for "find me anything on the web about X" — it discovers the URLs first and then reads them. |
| **Playwrighter** | A scripted, interactive browser-automation agent built on Playwright (Chromium/Firefox/WebKit). Where Crawler does a static one-shot HTTP fetch and Googler only searches, Playwrighter drives a REAL browser through an ordered list of steps — navigate, fill forms, click, press keys, wait for elements, extract text/attributes, screenshot, assert, download — so it can log into a site, submit a multi-step form, click through a wizard, scrape a JavaScript-rendered single-page-app behind a login, or run an end-to-end UI check. Set `headless: false` to watch it drive, and `hold_open_seconds: N` (alias `hold_open_ms`) to keep the browser visible for N seconds after the last step before it closes (the "wait before closing so I can see it" knob); use `storage_state_in`/`storage_state_out` to carry a logged-in session between runs. It writes an `INI_SECTION_PLAYWRIGHTER` block (`status`, `assert_result`, extracted values) so a downstream Forker can branch on pass/fail and Parametrizer can pipe scraped data onward, and always triggers `target_agents` (success or failure). The visual-canvas counterpart of the `chat_agent_playwrighter` Multi-Turn tool. |
| **Apirer** | An HTTP/REST API client agent. Performs a configurable GET / POST / PUT / DELETE request with arbitrary headers, body, and timeout, then logs the response status, body and round-trip latency in milliseconds. Authorization headers are masked in the log for security. Triggers downstream agents regardless of HTTP outcome (so retries and error branches are wired downstream, not inside the agent). |
| **Kalier** | Tlamatini's bridge to **Kali Linux** offensive-security tooling, built on the MCP-Kali-Server (https://www.kali.org/tools/mcp-kali-server/). On trigger it POSTs to the MCP-Kali-Server Flask API (`server.py`) running on a Kali box and runs ONE capability selected by its `action` field: `nmap` (port/service scan), `gobuster` / `dirb` (web content & directory busting), `nikto` (web-server scan), `sqlmap` (SQL-injection testing), `wpscan` (WordPress scan), `enum4linux` (SMB/Samba enumeration), `hydra` (credential brute-force), `john` (hash cracking), `metasploit` (run a module), `command` (any shell command on the Kali box), or `health` (probe the API server and which tools are installed). It captures the tool's stdout/stderr into an `INI_SECTION_KALIER` block (`action`, `endpoint`, `subject`, `return_code`, `success`, `timed_out`, `server_url`) so a downstream Forker can branch on `{success}` / `{return_code}` and Parametrizer can pipe the output onward; it ALWAYS triggers `target_agents` (success or failure). Point `server_url` at the running API server (default `http://127.0.0.1:5000`; for a remote Kali box use an SSH tunnel `ssh -L 5000:localhost:5000 user@KALI_IP`, or set it to `http://<KALI_LAN_IP>:5000`). In **chat (Multi-Turn)** you never repeat the URL — Tlamatini is the embedded MCP-Kali-Server client and auto-injects the **`kali_server_url`** you set once in **Config ▸ URLs** as the default `server_url` on every `chat_agent_kalier` run (the LLM may still pass `server_url=` to override for a one-off box); on the canvas you set `server_url` in this node's dialog. Implemented self-contained with the Python stdlib (`urllib`), so it needs no extra packages in the agent pool. **Authorized use only — run Kalier ONLY against systems you own or are explicitly authorized to test (engagement, lab, CTF).** The visual-canvas counterpart of the `chat_agent_kalier` Multi-Turn tool. |
| **Unrealer** | Drives an Unreal Engine 5 editor instance through the Unreal MCP plugin's TCP protocol (default 127.0.0.1:55557 — the plugin must already be running inside UE5; the agent does not start the engine). Sends one JSON command per execution (`{"type": <command>, "params": {...}}`) and captures the full Unreal response into an `INI_SECTION_UNREALER` block so Parametrizer can route specific fields into downstream agents. Forwards any command the connected plugin build exposes; the extended Unreal MCP surface is 53 commands across nine categories: actor manipulation incl. `focus_viewport` and `take_screenshot` (observe-your-change), Blueprint creation/editing incl. `set_pawn_properties`, Blueprint node-graph wiring incl. `find_blueprint_nodes`, input mappings, UMG widget building, **system** (`execute_python` — the universal in-editor escape hatch — plus `execute_console_command`, `get_class_info`, `list_assets`), **level/world** (`open_level` / `save_current_level` / `save_all` / `new_level` / `get_current_level`), **asset** (`import_asset` / `duplicate_asset` / `rename_asset` / `delete_asset` / `save_asset` / `create_folder`), and **material** (`create_material` / `create_material_instance` / `set_material_parameter` / `assign_material`). Headless build/cook/test is out of scope (that needs UnrealEditor-Cmd, not this editor socket). The visual-canvas counterpart of the wrapped `chat_agent_unrealer` Multi-Turn tool — same command surface, same response shape, fully unattended and chainable through Parametrizer. The recommended plugin to install is Tlamatini's own extended Unreal MCP fork — the Unreal Engine MCP modified specifically for this system — at https://github.com/XAIHT/XaihtUnrealEngineMCP.git (built on the upstream https://github.com/chongdashu/unreal-mcp; it ships the full 53-command surface). |
| **Blenderer** | Drives a Blender instance through the OFFICIAL Blender MCP add-on's TCP socket (default localhost:9876 — the add-on must already be running inside Blender with "Online access" enabled and the server started; the agent does not launch Blender). Unlike Unreal's verb-dispatch protocol, the Blender MCP wire format is a CODE-EXECUTION protocol — each run sends `{"type":"execute","code":<python>,"strict_json":<bool>}` (NUL-framed) and Blender runs that Python (which assigns a `result` dict) — so Blenderer exposes a RICH ACTION CATALOG selected by its `command` field instead of forcing hand-written Python for every task: PASSTHROUGH `execute_code` (run arbitrary `bpy` Python — the universal escape hatch); READ-ONLY `ping` / `scene_info` / `get_objects` / `get_object_detail` / `blendfile_summary`; and MUTATING/OUTPUT `create_object` (cube/sphere/cylinder/cone/plane/monkey/torus), `delete_object`, `set_material` (Principled base colour), `screenshot`, and `render` (file outputs default under Tlamatini's Temp directory). It captures the full Blender response into an `INI_SECTION_BLENDERER` block (`host`, `port`, `command`, `status`, `error`, `response_body`) so a downstream Forker can branch on `{status}` and Parametrizer can pipe fields onward; it ALWAYS triggers `target_agents` (success or failure). Implemented self-contained with the Python stdlib (raw NUL-byte-framed socket), so the agent pool needs no extra packages. Because Tlamatini speaks the add-on socket DIRECTLY (no separate `blmcp` MCP-server bridge and no external LLM client), it is a drop-in, far more integrated alternative to the bare chat client blender.org recommends — with the full Tlamatini canvas, Multi-Turn, Exec Report and Parametrizer pipelines on top. The visual-canvas counterpart of the wrapped `chat_agent_blenderer` Multi-Turn tool. |
| **STM32er** | Tlamatini's bridge to the **STM32 Template Project MCP** (https://github.com/XAIHT/STM32TemplateProjectMCP) — a self-contained scaffold + MCP server that lets a flow **scaffold → author → build → flash → observe** STM32F4 firmware programmatically, with **no STM32CubeIDE GUI**, using the GCC / CubeProgrammer / HAL toolchain bundled inside STM32CubeIDE. **Zero-config**: STM32er DOWNLOADS and installs the MCP itself on first use (shallow `git clone`, or a GitHub-zip fallback when git is absent, then pip-installs `mcp`/`pyserial` if missing, then validates) into a per-user cache, so the user installs **only STM32CubeIDE + Tlamatini** — `action='bootstrap'` (re)provisions it explicitly. Before any compile/flash it runs a **safety preflight** that validates the arm-none-eabi-gcc toolchain / STM32CubeIDE / build tool / programmer / ST-LINK USB driver + a positively-confirmed connected probe / target device family, and **REFUSES (fail-safe)** rather than producing or uploading mis-targeted firmware — compile-only actions need NO board, but flash / erase / reset / serial / SWD / live-memory require a connected ST-LINK, and a cross-STM32F-family device is refused; `action='validate'` reports the whole environment without building. On trigger it spawns that project's FastMCP **stdio** server (`mcp/stm32_mcp_server.py`), performs the MCP initialize handshake, and runs ONE capability selected by its `action` field, then captures the result into an `INI_SECTION_STM32ER` block (`action`, `tool`, `ok`, `returncode`, `success`, `project_dir`, `session_id`, `stage`, plus a `response_body` with the tool's stdout/stderr or JSON) so a downstream Forker can branch on `{success}` / `{returncode}` and Parametrizer can pipe `{project_dir}` (or live-memory samples) onward; it ALWAYS triggers `target_agents` (success or failure). The `action` covers all 23 MCP tools — environment (`get_config`, `discover_toolchain_tool`); project lifecycle (`create_project`, `write_source`, `read_source`, `list_sources`, `clean`); build & flash (`build`, `list_artifacts`, `flash`, `build_and_flash`, `erase`, `reset`); serial / VCP hardware-in-the-loop (`serial_list_ports`, `serial_connect`, `serial_send`, `serial_read`, `serial_disconnect`); and live SWD memory of a RUNNING target without halting it (`read_memory`, `write_memory`, `live_memory_start`, `live_memory_read`, `live_memory_stop`) — plus two composites that chain the stateful tools in one run: `serial_session` (connect → send/read → disconnect) and `live_monitor` (start → stream `monitor_seconds` → read → stop). A typical pipeline chains `create_project` → Parametrizer (carry `{project_dir}` forward) → `write_source` (drop generated `main.c`) → `build_and_flash` → a `live_monitor` / `serial_session` that proves the firmware runs on real silicon. Leave `server_script` **blank** for the zero-config auto-download (set it only to point at an existing local checkout — an on-disk path wins and skips the bootstrap); set `mcp_python` only if the Python running the server lacks the `mcp` / `pyserial` packages. On the canvas you set these (and `auto_bootstrap` / `mcp_repo_url` / `mcp_install_dir` / `preflight` / `device`) in this node's dialog. In **chat (Multi-Turn)** you never repeat anything — Tlamatini is the embedded MCP client and auto-injects the **`stm32_mcp_server_script`** (default empty = bootstrap) plus optional `stm32_mcp_python` / `stm32_template_dir` / `stm32_ide_root` / `stm32_mcp_repo_url` / `stm32_mcp_install_dir` you set once in **Config ▸ URLs** on every `chat_agent_stm32er` run. Implemented self-contained with the Python stdlib (raw MCP stdio JSON-RPC over `subprocess`), so the agent pool needs no extra packages — only the server does. **Authorized hardware only — `flash` / `erase` / `reset` / `write_memory` mutate a real attached MCU.** The visual-canvas counterpart of the `chat_agent_stm32er` Multi-Turn tool. |
| **ESP32er** | Tlamatini's bridge to **PlatformIO Core** (https://platformio.org) — the open-source embedded build system whose `pio` CLI lets a flow **scaffold → author → build → upload → observe** ESP32 (and ESP8266 / Espressif) firmware programmatically, with **no IDE**. Unlike STM32er (which drives a separate MCP server) PlatformIO already ships a complete CLI, so ESP32er invokes `pio` subcommands **directly** and captures their stdout/stderr. **Zero-config**: ESP32er DOWNLOADS and installs PlatformIO Core itself on first use (the official `get-platformio.py` installer, with a `pip install platformio` fallback) into a per-user cache, so the user installs **only the board USB driver + Tlamatini** — `action='bootstrap'` (re)provisions it explicitly. Before any build/upload it runs a **safety preflight** that validates `pio` is resolvable + a `platformio.ini` exists, and for an upload/monitor that a **serial port is actually connected** (ESP32 flashes over its onboard USB-serial bootloader — no external JTAG probe needed), and **REFUSES (fail-safe)** rather than running a build/upload that cannot succeed; `action='validate'` reports the whole environment without building. On trigger it runs ONE capability selected by its `action` field, then captures the result into an `INI_SECTION_ESP32ER` block (`action`, `tool`, `ok`, `returncode`, `success`, `project_dir`, `port`, `environment`, `stage`, plus a `response_body` with the `pio` stdout/stderr) so a downstream Forker can branch on `{success}` / `{returncode}` and Parametrizer can pipe `{project_dir}` onward; it ALWAYS triggers `target_agents` (success or failure). The `action` covers: environment/meta (`bootstrap`, `validate`, `system_info`, `boards`); project lifecycle (`create_project`, `write_source`, `read_source`, `list_sources`, `clean`); build & flash (`build`, `upload`, `build_and_upload`, `list_artifacts`); serial hardware-in-the-loop (`device_list`, `monitor` — a bounded `pio device monitor` drained for `monitor_seconds`, and `monitor_session` = upload → monitor in one run); and packages / QA (`pkg_install`, `pkg_list`, `pkg_update`, `check`, `test`). A typical pipeline chains `create_project` (`board: esp32dev`) → Parametrizer (carry `{project_dir}` forward) → `write_source` (drop generated `src/main.cpp`) → `build` → `upload` → a `monitor` / `monitor_session` that proves the firmware runs on real silicon. Leave `pio_executable` **blank** for the zero-config auto-install (set it only to point at an existing PlatformIO binary — it wins and skips bootstrap). On the canvas you set `action` / `project_dir` / `board` / `framework` / `environment` / `port` (and `auto_bootstrap` / `pio_core_dir` / `preflight`) in this node's dialog. In **chat (Multi-Turn)** you never repeat anything — Tlamatini auto-injects the **`pio_executable`** / **`pio_core_dir`** you set once in **Config ▸ URLs** on every `chat_agent_esp32er` run. Implemented self-contained with the Python stdlib (`subprocess` + `urllib`), so the agent pool needs no extra packages. NOTE: the FIRST build downloads the espressif32 platform + toolchain (hundreds of MB) so it is slow. **Authorized hardware only — `upload` mutates a real attached MCU.** The visual-canvas counterpart of the `chat_agent_esp32er` Multi-Turn tool. |
| **Arduiner** | Tlamatini's bridge to the **Arduino CLI** (https://arduino.github.io/arduino-cli/) — the official all-in-one tool whose `arduino-cli` binary lets a flow **scaffold → author → build → upload → observe** classic-Arduino / AVR / SAMD / Arduino-core firmware programmatically, with **no IDE**. The third microcontroller agent, it shares the ESP32er scheme: like PlatformIO's `pio` (and unlike STM32er's MCP server), arduino-cli is itself a complete CLI, so Arduiner invokes `arduino-cli` subcommands **directly** and captures their stdout/stderr. **The microcontroller is selected by the `fqbn`** (Fully Qualified Board Name, e.g. `arduino:avr:uno`, `arduino:avr:mega2560`, `arduino:samd:mkr1000`, `esp32:esp32:esp32`); the serial `port` and `baud` set the upload/monitor link. **Zero-config**: Arduiner DOWNLOADS and installs the arduino-cli binary itself on first use (the platform release archive from downloads.arduino.cc, unzipped into a per-user cache, then `config init` + `core update-index`), so the user installs **only the board USB driver + Tlamatini** — `action='bootstrap'` (re)provisions it explicitly. Because arduino-cli — unlike PlatformIO — does NOT auto-install platforms on compile, Arduiner **AUTO-INSTALLS the board's core** for the FQBN before a build (`auto_core_install`, default true); for THIRD-PARTY silicon (ESP32/STM32/RP2040) set `additional_urls` to the vendor's `package_*_index.json`. Before any build/upload it runs a **safety preflight** that validates `arduino-cli` is resolvable + a sketch (`.ino`) + an FQBN exist, and for an upload/monitor that a **serial port is actually connected** (Arduino-family boards flash over USB-serial — no external probe needed for the common path), and **REFUSES (fail-safe)** rather than running a build/upload that cannot succeed; `action='validate'` reports the whole environment without building. On trigger it runs ONE capability selected by its `action` field, then captures the result into an `INI_SECTION_ARDUINER` block (`action`, `tool`, `ok`, `returncode`, `success`, `fqbn`, `port`, `sketch_path`, `stage`, plus a `response_body` with the `arduino-cli` stdout/stderr) so a downstream Forker can branch on `{success}` / `{returncode}` and Parametrizer can pipe `{sketch_path}` onward; it ALWAYS triggers `target_agents` (success or failure). The `action` covers: environment/meta (`bootstrap`, `validate`, `system_info`, `boards`, `device_list`); cores & libraries (`core_update_index`, `core_search`, `core_list`, `core_install`, `core_uninstall`, `lib_update_index`, `lib_search`, `lib_list`, `lib_install`); project lifecycle (`create_project`, `write_source`, `read_source`, `list_sources`); build & flash (`build`, `upload`, `build_and_upload`, `clean`, `list_artifacts`); serial hardware-in-the-loop (`monitor` — a bounded `arduino-cli monitor --config baudrate=<baud>` drained for `monitor_seconds`, and the composite `monitor_session` = upload → monitor in one run). `create_project` scaffolds from the bundled **ArduinoTemplateProject** (the Arduino-family analog of STM32er's STM32 Template Project and ESP32er's `pio` scaffold) — it copies the template, renames the `.ino` to the destination folder, and stamps the FQBN/port into the template's `sketch.yaml` profile. A typical pipeline chains `create_project` (`fqbn: arduino:avr:uno`) → Parametrizer (carry `{sketch_path}` forward) → `write_source` (drop generated `.ino`) → `build` → `upload` → a `monitor` / `monitor_session` that proves the firmware runs on real silicon. Leave `arduino_cli_executable` **blank** for the zero-config auto-install (set it only to point at an existing arduino-cli binary — it wins and skips bootstrap). On the canvas you set `action` / `fqbn` / `sketch_path` / `port` (and `auto_bootstrap` / `arduino_cli_install_dir` / `auto_core_install` / `additional_urls` / `preflight`) in this node's dialog. In **chat (Multi-Turn)** you never repeat anything — Tlamatini auto-injects the **`arduino_cli_executable`** / **`arduino_cli_install_dir`** you set once in **Config ▸ URLs** on every `chat_agent_arduiner` run. Implemented self-contained with the Python stdlib (`subprocess` + `urllib` + `zipfile`/`tarfile`), so the agent pool needs no extra packages. **Authorized hardware only — `upload` mutates a real attached MCU.** The visual-canvas counterpart of the `chat_agent_arduiner` Multi-Turn tool. |
| **ESPHomer** | Tlamatini's bridge to **ESPHome** (https://esphome.io) — the system that turns ESP32 / ESP8266 / RP2040 / BK72xx boards into smart-home devices from a **simple YAML config (NO C++)** — letting a flow **author → validate → compile → upload → observe** device firmware programmatically, with **no IDE**. Like ESP32er (PlatformIO) and Arduiner (arduino-cli), and unlike STM32er (which drives a separate MCP server), ESPHome ships a complete CLI, so ESPHomer invokes `esphome` subcommands **directly** and captures their stdout/stderr. **Zero-config**: ESPHomer INSTALLS ESPHome itself on first use (`pip install esphome`) into the agent's Python, so the user installs **only the board USB driver + Tlamatini** — `action='bootstrap'` (re)provisions it explicitly. Because the interactive `esphome wizard` can't run headless, ESPHomer ships a built-in **`new_config`** generator that writes a minimal, valid device YAML (a phone-controllable on/off light, with the native API / OTA / WiFi blocks) from `name` / `platform` / `board` / `wifi_ssid` / `wifi_password` / `led_pin`. Before any compile/upload it runs a **safety preflight** that validates `esphome` is resolvable + a device YAML exists, and for an upload/logs/run that a **serial port is connected** (first flash is over USB) **or** an OTA host is given in `port` (subsequent updates go over WiFi), and **REFUSES (fail-safe)** rather than running a build/upload that cannot succeed; `action='validate'` reports the whole environment without building. On trigger it runs ONE capability selected by its `action` field, then captures the result into an `INI_SECTION_ESPHOMER` block (`action`, `tool`, `ok`, `returncode`, `success`, `config_path`, `name`, `port`, `stage`, plus a `response_body` with the `esphome` stdout/stderr) so a downstream Forker can branch on `{success}` / `{returncode}` and Parametrizer can pipe `{config_path}` onward; it ALWAYS triggers `target_agents` (success or failure). The `action` covers: environment/meta (`bootstrap`, `validate`, `version`); device YAML lifecycle (`new_config`, `write_config`, `read_config`, `config`, `clean`); build & flash (`compile`, `upload`, `run`, `list_artifacts`); serial/OTA HIL (`logs` — a bounded `esphome logs` drained for `monitor_seconds`); plus the one-call composite `scaffold_compile_upload` (author → config → compile → upload → logs in a SINGLE run). A typical pipeline chains `new_config` → Parametrizer (carry `{config_path}` forward) → `compile` → `upload` → a `logs` that proves the device runs. Leave `esphome_executable` **blank** for the zero-config auto-install (set it only to point at an existing esphome binary — it wins and skips bootstrap). On the canvas you set `action` / `config_path` / `name` / `platform` / `board` / `port` (and `auto_bootstrap` / `preflight` / WiFi fields) in this node's dialog. In **chat (Multi-Turn)** you never repeat anything — Tlamatini auto-injects the **`esphome_executable`** you set once in **Config ▸ URLs** on every `chat_agent_esphomer` run. Implemented self-contained with the Python stdlib (`subprocess` + `glob`), so the agent pool needs no extra packages. NOTE: the FIRST compile downloads the platform + toolchain (via PlatformIO under the hood) so it is slow. **Authorized hardware only — `upload` mutates a real attached device.** The visual-canvas counterpart of the `chat_agent_esphomer` Multi-Turn tool. |
| **Gitter** | A Git operations agent. Runs `clone`, `pull`, `push`, `commit`, `checkout`, `branch`, `diff`, `log`, `status`, or any custom git command against a local repository, capturing both stdout and stderr into a structured `<git {command}> RESPONSE { ... }` block. Use it for unattended sync, CI-style builds that pull before they run, scheduled commits of generated artifacts, or audit trails of repository state. |
| **Reviewer** | An LLM-powered code reviewer. On trigger it resolves a git diff for the configured `repo_path` — a commit range like `HEAD~1`, a branch like `origin/main`, or (with an empty `diff_ref`) your uncommitted working-tree and staged changes — then sends that diff to an Ollama model with a rigorous senior-engineer review prompt. It writes an `INI_SECTION_REVIEWER` block whose first field is a **verdict** (`APPROVE`, `REQUEST_CHANGES`, or `COMMENT`), followed by line-anchored findings spanning correctness, security, performance and readability. Because the verdict is a header field, a downstream Forker can branch the whole flow on it — e.g. auto-merge on APPROVE, open an issue on REQUEST_CHANGES. The visual-canvas counterpart of the `code-review` skill. |
| **Analyzer** | A deterministic static-analysis and security scanner — it uses no LLM, so its output is reproducible. On trigger it runs whichever of `bandit`, `semgrep`, `ruff`, `eslint`, `gitleaks` and `pip-audit` are installed on PATH over the configured `target_path`, aggregates their findings, and writes an `INI_SECTION_ANALYZER` block whose `status` is `clean`, `findings`, or `error` and whose `total_findings` count lets a downstream Forker gate the flow (for example, block a deploy when secrets or high-severity issues are found). Scanners that aren't installed are listed as skipped rather than failing the run. The visual-canvas counterpart of the `security-audit` skill. |
| **Ssher** | Runs a shell command on a remote host over SSH using a pre-configured key. Captures stdout and stderr and triggers `target_agents` on success. The "Executer for somebody else's box" — pair it with Scper to push a script first and then run it remotely. |
| **Scper** | Transfers a file to or from a remote host over SCP using a pre-configured SSH key. Direction is either `send` (local → remote) or `receive` (remote → local). The file-transfer companion of Ssher; together they cover almost every "do something on the staging server" scenario. |
| **Dockerer** | A Docker / docker-compose front-end. Runs `up`, `down`, `restart`, `stop`, `build`, `logs`, `ps`, `pull`, etc. against a configured `compose_file` or against the local Docker daemon. Use it to spin a container up before a test runs, stream logs into a flow, or tear down environments at the end of a run. |
| **Kuberneter** | A `kubectl` front-end. Runs commands like `get`, `apply`, `logs`, `exec` against a configured namespace, with arbitrary extra arguments or a fully custom command line. Lets a flow inspect, deploy to, or debug a Kubernetes cluster as a normal step in your canvas. |
| **Pser** | An LLM-powered process finder. Given a `likely_process_name` (e.g. "vscode" or "the python that runs my web server"), it lists running processes and asks an Ollama model to identify the best semantic match. Useful when an automation needs to react to "the user's editor" or "their dev server" without knowing the exact binary name in advance. |
| **Jenkinser** | Triggers a job on a Jenkins server. Handles the CSRF crumb dance, posts build parameters, logs the response, and starts downstream agents regardless of trigger result. Use it inside any flow that needs to nudge a CI pipeline (e.g. "after my commits land, kick the deploy job"). |
| **Prompter** | Sends a configured prompt to an Ollama LLM and logs the response into a structured `INI_SECTION_PROMPTER` block. This is the simplest "ask the local LLM something" agent — use it for one-shot generation steps that downstream agents can then parse, or as the LLM-side of a Parametrizer chain. |
| **Summarizer** | Two modes in one agent. **Polling mode**: continuously watches the logs of its `source_agents` and feeds new content to an Ollama model with a configurable system prompt; when the LLM detects a positive event (writes `[EVENT_TRIGGERED]`), it fires `target_agents`. **One-shot mode** (when `input_text` is provided directly): summarizes that text once with a target word count and emits a single `INI_SECTION_SUMMARIZER` block. Pair it with File-Interpreter or Crawler to compress long content into something a downstream agent can act on. |
| **File-Interpreter** | Reads and interprets documents — DOCX, PPTX, XLSX, PDF, TXT, TeX, CSV, HTML, RTF, and more — extracting text and (optionally) embedded images. Three reading modes: `fast` (text only), `complete` (text + images dumped to a sibling `images/` folder), and `summarized` (text + LLM-generated summary). Outputs neatly structured INI/END_FILE blocks per file. Wildcards are supported, so a single agent can process an entire folder of documents in one run. |
| **File-Extractor** | The "give me the raw text" sibling of File-Interpreter. Reads a path or wildcard pattern, extracts plaintext using the same multi-format support, and falls back to a `strings`-style scan for unknown binary types. No LLM, no summarization, no image dumping — just text. Ideal as the first hop in a pipeline that ends in Parametrizer → another agent's `buffer` field. **Claude-Read-style VIEW options** (all optional, default to the full text): `line_numbers` prefixes each line with its 1-based file line number; `offset` (1-based start line) + `limit` (max lines) return just a slice - read lines 200-260 of a big file without dumping the whole thing. |
| **Image-Interpreter** | Vision-LLM image describer. Sends one or more images to a vision-capable Ollama model with a custom (or default) system prompt and writes a textual description into structured `INI_IMAGE_FILE` blocks. Accepts wildcards, a directory, a File-Interpreter pool name, or a single file. Supports 12+ image formats (JPG, PNG, GIF, BMP, TIFF, WebP, SVG, ICO, HEIC, AVIF). The eyes of any flow that needs to look at screenshots, charts, or photos. |
| **J-Decompiler** | A Java decompiler. Scans a directory (or wildcard list) for `.class`, `.jar`, `.war`, and `.ear` files and uses the bundled `jd-cli` tool to generate human-readable Java sources next to each artefact (or into sibling directories for archives). Use it to inspect third-party JARs, recover sources from build artefacts, or audit obfuscated code. |
| **De-Compresser** | Deterministic short-running archive worker. Direction is inferred from the extensions: if `input` is a `.gz`, `.7z`, `.zip`, `.tar.gz`, or `.gz.tar` file the agent **decompresses** into the `output` directory; if `output` ends in one of those extensions the agent **compresses** `input` (a file or a directory) into `output`. Handles GNU Zip (`.gz`), universal ZIP (`.zip`), LZMA/LZMA2 7-Zip (`.7z`) and gzipped tar (`.tar.gz` / `.gz.tar`). Password handling: set `passwordless=true` to skip; set `passwordless=false` and the agent reads the password from the OS environment variable `DE_COMPRESSER_PWD` — if that variable is undefined the agent fails fast and still triggers any `target_agents`. Use it whenever the user wants to pack/unpack files inside a flow without dropping into `chat_agent_executer` + `tar`/`zip` CLIs. |
| **Mover** | Moves or copies files. Accepts a list of source patterns (with wildcards), a destination folder, an `operation` (move / copy), an optional `recursive` flag for sub-directory traversal, and `filetype_exclusions` to skip certain extensions or filenames. Use it to herd outputs into the right folder, archive logs, or stage artefacts for the next pipeline step. |
| **Deleter** | Deletes files matching one or more patterns (with wildcards) from a target location. Supports `recursive` directory descent, `filetype_exclusions`, and two modes: `immediate` (delete on start) or `event` (delete only when an upstream pattern is detected). Use it for cleanup phases, scratch-space management, or pattern-driven log rotation. |
| **File-Creator** | Writes a file with arbitrary content to a specified path, then triggers downstream agents (regardless of the write outcome). The content is taken verbatim from the YAML config, so you can use it to write JSON, scripts, fixtures, prompt templates — anything. Often the last step of a Parametrizer chain that materializes an LLM-generated artefact onto disk. **Preferred over Keyboarder / Mouser for ALL source-file / script / config authorship; never open Notepad / VS Code / an IDE and type the file through Keyboarder — File-Creator writes the file atomically in one step.** |
| **Shoter** | Takes a screenshot of the primary display and saves it to `output_dir`. Read-only and side-effect-free, so it does not appear in the Exec Report. Combine it with Image-Interpreter to give the LLM "eyes" on the desktop, or use it to capture proof-of-execution snapshots inside CI-style flows. |
| **Globber** | Read-only FILE discovery by glob/filename pattern under a directory (the Claude-Glob equivalent) - returns matching file paths, newest-first by default. Pass `pattern` (a glob like `*.py` or `**/*.md`; `**` searches recursively) and `path` (the base directory); optionally `sort_by` (`mtime` = newest modified first [default], `name` = alphabetical, `none`) and `max_results` (cap, default 500). Returns files only (not directories) and never changes anything. Emits an `INI_SECTION_GLOBBER` block (`pattern`, `path`, `matches`, `truncated`, `status`) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{status}`. Use it to DISCOVER which files exist or were recently changed before reading, grepping, or editing them. The visual-canvas counterpart of the `chat_agent_globber` Multi-Turn tool. |
| **Grepper** | Read-only regex CONTENT search across a single file or a whole directory tree (the Claude-Grep equivalent) - returns matching lines as `file:line:match`. Pass `pattern` (a Python regex) and `path` (the file or directory to search); optionally `glob` (a basename filter like `*.py`), `case_insensitive`, `output_mode` (`content` = file:line:match lines [default], `files` = matching paths only, `count` = per-file counts), and `max_results` (cap, default 200). Noise directories (.git, node_modules, venv, __pycache__, dist, build) are pruned and binary/unreadable files are skipped automatically. Never changes any file. Emits an `INI_SECTION_GREPPER` block (`pattern`, `path`, `glob`, `matches`, `files_searched`, `truncated`, `status`) and ALWAYS triggers `target_agents` so a downstream Forker can branch on `{status}`. Use it to FIND where something appears before reading or editing it. The visual-canvas counterpart of the `chat_agent_grepper` Multi-Turn tool. |
| **Editor** | Makes a SURGICAL in-place edit to a single existing text file by replacing an EXACT string (`old_string`) with another (`new_string`) — the find-and-replace equivalent that changes a file WITHOUT rewriting it (prefer it over File-Creator when only part of an existing file should change). By default `old_string` must be UNIQUE in the file; it refuses on >1 match unless `replace_all` is true. For code with backslashes or quotes, pass `old_string_b64`/`new_string_b64` (base64) for a byte-exact channel. The edit preserves the file's exact line endings. Emits an `INI_SECTION_EDITOR` block (`file_path`, `status` edited/not_found/not_unique/noop/error, `occurrences`, `replacements`) and ALWAYS triggers `target_agents` so a downstream Forker can branch on the outcome. State-changing, so it appears in the Exec Report. The visual-canvas counterpart of the `chat_agent_editor` Multi-Turn tool. |
| **Camcorder** | Captures from a SYSTEM CAMERA (webcam) via OpenCV — distinct from Shoter, which captures the SCREEN. Takes a single PHOTO (the default) or records a VIDEO segment of `video_duration_seconds` (no audio), saving the file with a timestamped, collision-proof name. By default the file lands in the user's Pictures folder under `TlamatiniCamcorder`; set `output_dir` to override. Pick a non-default camera with `camera_index` (0 = default). Resolution is optional — leave `resolution_width`/`resolution_height` at 0 to use the camera's native resolution (recommended; a webcam only supports a discrete set of modes), or request a specific one. Emits an `INI_SECTION_CAMCORDER` block (`output_path`, `output_dir`, `filename`, `media_type`, `camera_index`, `duration_seconds`, `resolution`, `fps`) so a downstream Parametrizer/Image-Interpreter can read the exact saved path, and always triggers `target_agents`. Read-only/observational like Shoter, so it does not appear in the Exec Report. The visual-canvas counterpart of the `chat_agent_camcorder` Multi-Turn tool. |
| **Recorder** | Records AUDIO from a system input device (MICROPHONE) via `sounddevice` and saves a WAV (written with the stdlib `wave` module) — the audio sibling of Camcorder (camera) and Shoter (screen). By default it records from the SYSTEM DEFAULT mic for `record_seconds` seconds; pick a specific mic on a multi-microphone machine with `device_index` (the agent logs the numbered input-device list at startup) or by name with `device_name`. Sampling rate is optional — leave `sample_rate` at 0 to use the device's native default rate (recommended; the device always supports it), or force `44100`/`48000`/`16000`. `channels` defaults to mono (1) and is clamped to the device's max. `input_gain_percent` applies a software/digital gain (100 = unity default, 200 = louder, 50 = quieter); it is post-capture scaling, not the hardware mic level, so amplifying may clip — the clipped-sample count is reported. By default the file lands in the user's Music folder under `TlamatiniRecords` with a timestamped, collision-proof name; set `output_dir` to override. Emits an `INI_SECTION_RECORDER` block (`output_path`, `output_dir`, `filename`, `device_index`, `device_name`, `sample_rate`, `channels`, `duration_seconds`, `gain_percent`, `clipped_samples`, `format`) so a downstream Parametrizer can read the exact saved path, and always triggers `target_agents`. Read-only/observational like Shoter/Camcorder, so it does not appear in the Exec Report. The visual-canvas counterpart of the `chat_agent_recorder` Multi-Turn tool. |
| **Whisperer** | SPEECH-TO-TEXT (STT / voice recognition): turns SPOKEN AUDIO into a STRING of text — the speech-to-text sibling of Talker (text-to-speech). It is 100% SELF-SUFFICIENT for the microphone: it OPENS, CONFIGURES (channels, sample rate, software gain) and RECORDS the mic ITSELF (it does NOT depend on the Recorder agent), or transcribes a given audio FILE when `audio_file` is set (`input_source` mic/file/auto). By default (`input_source: mic`) it records the SYSTEM DEFAULT mic for `record_seconds` seconds; pick another mic with `device_index` (the agent logs the numbered input-device list at startup) or `device_name`. Transcription uses **faster-whisper LOCALLY by default** (`engine: faster-whisper`): it AUTO-DETECTS an NVIDIA GPU (CUDA) and **ALWAYS falls back to CPU** on a machine without one (so it works everywhere), with `model` ∈ tiny/base/small/medium/large-v3/large-v3-turbo (default `base`) and `device` auto/cuda/cpu. For cloud STT set `engine` to `cloud-groq` or `cloud-openai` (key from config or `GROQ_API_KEY`/`OPENAI_API_KEY`). `language` "" auto-detects; `task: translate` outputs English. **Ollama CANNOT transcribe** (it has no audio input), so the recognition is always done by the ASR engine; setting `ollama_cleanup: true` only post-processes the FINISHED transcript's punctuation/casing with an Ollama LLM. Emits an `INI_SECTION_WHISPERER` block (`transcript_path`, `audio_path`, `input_source`, `engine`, `model`, `device`, `language`, `duration_seconds`, `segments`, `word_count`, `status`, plus the transcript text as the body) so a downstream Parametrizer/Forker can read the recognized text and branch on `{status}` (transcribed/empty/engine_unavailable/error); always triggers `target_agents` (success or failure). Read-only/observational (audio in → text out), so it does not appear in the Exec Report. **NOTE**: local transcription needs `faster-whisper` (`pip install faster-whisper`); if it is absent and no cloud key is set, Whisperer reports `status: engine_unavailable` rather than crashing. The visual-canvas counterpart of the `chat_agent_whisperer` Multi-Turn tool. |
| **AudioPlayer** | PLAYS an audio file through a system audio OUTPUT device (speakers / audio out) via `soundfile` + `sounddevice` — the playback counterpart of the media family (Shoter = screen, Camcorder = camera, Recorder = microphone-IN, AudioPlayer = speakers-OUT). Set `audio_file` to the path of the file to play (WAV/FLAC/OGG/AIFF, and MP3 with a recent libsndfile); the full absolute path of the played file is logged and emitted. By default it plays to the SYSTEM DEFAULT output device; pick another with `device_index` (the agent logs the numbered output-device list at startup) or by name with `device_name`. `volume_percent` is a SOFTWARE gain (100 = unity default, 200 = louder, 50 = quieter — NOT the OS volume slider; amplifying may clip and the clipped-sample count is reported). `time_played` sets how many SECONDS to play: 0 (default) plays the WHOLE file once; a positive value plays EXACTLY that long — a longer file is TRUNCATED, a shorter file is LOOPED (repeated to fill the time with a final partial segment), done with a streaming callback so a huge duration over a tiny file never allocates a giant buffer. Sampling rate is optional — leave `sample_rate` at 0 to play at the file's own native rate (recommended; correct pitch), or force a value (which alters pitch/tempo, as the audio is not resampled). It does NOT change the Windows OS default audio device. Emits an `INI_SECTION_AUDIOPLAYER` block (`input_path`, `input_dir`, `filename`, `device_index`, `device_name`, `file_sample_rate`, `play_sample_rate`, `channels`, `volume_percent`, `clipped_samples`, `file_duration_seconds`, `time_played_requested`, `played_seconds`, `play_mode`, `loops`, `partial_segment`, `format`, `status`) so a downstream Parametrizer/Forker can read the exact path, the time played, and branch on `{status}`; always triggers `target_agents` (success or failure). Observational/output (mutates no persistent state) like Shoter/Recorder/Camcorder, so it does not appear in the Exec Report. The visual-canvas counterpart of the `chat_agent_audioplayer` Multi-Turn tool. |
| **VideoPlayer** | PLAYS a video file (WITH audio) on a chosen DISPLAY (screen) — the on-screen sibling of AudioPlayer (speakers). Decodes any ffmpeg container (.mp4/.mov/.mkv/.avi/.webm) via `ffpyplayer` (whose pip wheel bundles ffmpeg+SDL — no external ffmpeg, no runtime download) and draws the window with the already-bundled OpenCV; if ffpyplayer is unavailable it falls back to SILENT OpenCV-only video (volume becomes a no-op, logged). Set `video_file` to the path of the file to play; the full absolute path of the played file is logged and emitted. `display_index` picks the monitor (-1 = primary, the default; the agent logs the numbered display list at startup). `volume_percent` is the audio volume (100 = full default, 50 = half, 0 = muted; over 100 capped at 100). `time_played` sets how many SECONDS to play: 0 (default) plays the WHOLE video once; a positive value plays EXACTLY that long — a longer video is TRUNCATED, a shorter one is LOOPED (repeated to fill the time with a final partial segment). `window_width`/`window_height` set the window size in pixels (0 = the video's native size); `fullscreen: true` fills the chosen display (ignoring the window size); `keep_aspect: true` (default) letterboxes instead of stretching. **Additional-params analysis**: it OMITS playback-speed/fps (native timing is used), decode-resolution/codec (intrinsic to the file; the window size controls only display scaling), and audio-track/subtitle selection (advanced) — all rarely needed. Emits an `INI_SECTION_VIDEOPLAYER` block (`input_path`, `input_dir`, `filename`, `display_index`, `display_geometry`, `video_width`, `video_height`, `window_width`, `window_height`, `fullscreen`, `volume_percent`, `backend`, `has_audio`, `file_duration_seconds`, `time_played_requested`, `played_seconds`, `play_mode`, `loops`, `partial_segment`, `format`, `status`) so a downstream Parametrizer/Forker can read the exact path, the time played, and branch on `{status}`; always triggers `target_agents` (success or failure). Observational/output (mutates no persistent state) like Shoter/AudioPlayer, so it does not appear in the Exec Report. The visual-canvas counterpart of the `chat_agent_videoplayer` Multi-Turn tool. |
| **Talker** | TEXT-TO-SPEECH (TTS): SPEAKS text aloud through a system audio OUTPUT device (speakers) by driving an OLLAMA connection that runs a neural TTS model (default `Orpheus-3b-FT`) — the voice-synthesis sibling of the media family (AudioPlayer plays an existing FILE; Talker GENERATES speech from text). Set `input_text` to the words to pronounce. **FEMALE VOICE ONLY (Tlamatini is female): a male voice is FORBIDDEN BY DESIGN, with no override.** `voice` picks one of the permitted FEMALE Orpheus voices: tara [default], leah, jess, mia, zoe (`gender` accepts only `female`, used when `voice` is empty). Requesting a male voice (leo/dan/zac) or `gender: male` makes Talker close its execution entirely and report "male voice is forbidden by design — NOW CLOSING.. BYE" — it never substitutes and never sounds male. `emotion` weaves a paralinguistic tag into the speech (laugh/chuckle/sigh/cough/sniffle/groan/yawn/gasp) — or inline `<laugh>` etc. directly in `input_text`. `language` passes a hint to the model (the base model is English-only; a multilingual fine-tune speaks others). `model`/`ollama_url`/`ollama_token` configure the Ollama connection; generation knobs are `temperature`, `top_p`, `top_k`, `min_p`, `repetition_penalty` (keep ≥ 1.1), `max_tokens`, `seed`. The model streams audio TOKENS that are decoded to a 24 kHz waveform by the SNAC neural codec; the WAV is saved to `output_dir` (default the user's Music folder under `TlamatiniTalker`) and played on the speakers (`device_index`/`device_name`/`volume_percent`/`sample_rate`; `play_audio: false` saves without playing). Emits an `INI_SECTION_TALKER` block (`output_path`, `output_dir`, `filename`, `model`, `language`, `voice`, `gender`, `emotion`, `sample_rate`, `audio_seconds`, `char_count`, `played`, `status`) so a downstream Parametrizer/Forker can read the spoken-WAV path and branch on `{status}`; always triggers `target_agents` (success or failure). Observational/output (mutates no persistent state), so it does not appear in the Exec Report. **NOTE**: fetching the tokens needs only the stdlib, but rendering audible audio needs `snac` + `torch` (a neural vocoder) installed (`pip install snac torch`); without them Talker saves the audio tokens and reports `status: tokens_only`. The visual-canvas counterpart of the `chat_agent_talker` Multi-Turn tool. |
| **Mouser** | Programmatic mouse control via PyAutoGUI. Either moves the pointer randomly for a duration (anti-idle behavior) or to a specific screen coordinate ("localized" mode); in localized mode it can also issue a configured click after the destination is reached (none / left / right / middle / double-left / double-right / double-middle). Use it to drive desktop UIs from a flow when no API is available. **Reserved for genuine desktop-UI automation explicitly requested by the user (mouse / pointer / click demos, anti-idle motion, GUI replay). Never use Mouser as part of a code-authoring flow — File-Creator writes the file, Executer / Pythonxer runs it; none of those need pointer events.** |
| **Windower** | The window manager of the desktop-UI trio (Windower = the window itself, Mouser = clicks inside it, Keyboarder = typing into it). Locates an application window by title and runs ONE lifecycle operation on it — `focus`/raise, `minimize`, `maximize`, `restore`, `move`, `resize`, `move_resize`, `close`, `topmost`/`untopmost`, or `arrange` (snap/tile to a screen region) — or `list`s every open window with its position, size and state. Matching is by `substring` (default), `exact`, or `regex`, with `match_index` to pick among same-titled windows. Implemented self-contained with the Win32 API (pywin32), including the reliable cross-process focus-transfer (AttachThreadInput) ported from Microsoft's Windows-MCP. Emits an `INI_SECTION_WINDOWER` block (`action`, `window_title`, `matched`, `match_count`, `state`, `left`, `top`, `width`, `height`) so a downstream Forker can branch on `{matched}`/`{state}` and Parametrizer can pipe geometry onward; always triggers `target_agents`. **Use Windower — not Mouser — whenever the goal is the window as a whole (bring to front, tile, resize, close by title); Mouser only clicks controls inside a window.** The visual-canvas counterpart of the `chat_agent_windower` Multi-Turn tool. |
| **Keyboarder** | Programmatic keyboard control via PyAutoGUI. Sends a sequence of keystrokes that mixes literal text (single- or double-quoted), bare key names (e.g. `enter`, `tab`, `f5`), and `+`-joined chords (e.g. `ctrl+s`, `ctrl+shift+t`), with a configurable inter-stroke delay. Pair it with Mouser and Shoter to automate any GUI app for which there is no scripting interface. **Reserved for genuine keyboard automation explicitly requested by the user (Notepad-typing demos, hotkey injection into third-party apps, GUI replays). Never use Keyboarder to author source code, scripts, configs, or any file content — use File-Creator (writes the file atomically), Pythonxer (runs inline Python), or Executer (runs a shell / build command) instead.** |

### Notification Agents

These are *terminal* agents — they react to upstream events but do NOT start anything else. Their job is to tell the outside world that something happened.

| Agent | Description |
|-------|-------------|
| **Notifier** | The browser-notification agent. Watches its `source_agents` for any of a comma-separated list of `search_strings` and, on a match, drops a `notification.json` file that the Tlamatini chat UI polls and renders as an in-browser DOM popup (with optional sound). Per-source file offsets and reanim files mean it never re-processes old log content even across restarts. |
| **Emailer** | Sends an SMTP email when triggered. Configurable sender, password, list of recipients, subject, and body. Common pattern: wire a Raiser to detect the success or failure of an upstream pipeline and have it trigger Emailer to notify a team mailing list. |
| **RecMailer** | An IMAP email receiver with LLM analysis. Polls a configured inbox, downloads new messages, and asks an Ollama model whether the body matches one of your `keywords_or_phrases`. Logs every match with the configured `outcome_word`. Use it to make a flow react to a real email arriving, e.g. an alert from an external SaaS. |
| **WhatsApper** | Sends WhatsApp messages via the **TextMeBot** API in response to log patterns. Watches its `source_agents` in parallel threads, summarizes any matched issue with an Ollama model, and posts the summary to a configured phone number. Handy as a "page my own phone when the build breaks" channel. |
| **Telegramer** | Sends a Telegram message via a Telegram bot token to a configured `chat_id`. The lightweight Telegram counterpart of Emailer — perfect for personal alerts, build-bot posts to a team channel, or anything where a Telegram message is the right shape of notification. |
| **TelegramRX** | Receives messages on a Telegram bot and writes them into the agent's log so downstream agents (typically a Raiser) can react. Use it to make a flow listen for commands sent to a bot, e.g. "type `/deploy` in Telegram and the production deploy chain runs". |
| **Monitor Log** | A pure log-file watcher with LLM event detection. Polls a configured `logfile_path`, feeds new lines to an Ollama model, and logs `outcome_word` (default `TARGET_FOUND`) when a match is detected. Designed to be the upstream `source_agents` of a Raiser — together they form the canonical "react to a log line" mini-flow. |
| **Monitor Netstat** | The network-port version of Monitor Log. Polls the system's open TCP/UDP connections, feeds them to the LLM, and emits `TARGET_FOUND` when the configured network condition matches. Use it for "alert me if port X opens" or "fire when no connection to host Y exists for N seconds". |
| **FlowHypervisor** | The flow's own watchdog. A system-managed agent that watches every other agent's log on a configurable poll interval, asks an Ollama model whether the flow as a whole is healthy, and writes either `OK` or `ATTENTION NEEDED { explanation }` to its own log. The chat UI reads those entries and surfaces problems as ATTENTION! alerts. Auto-stops when there are no other agents running, and self-stops after 3 idle cycles as a safety net. |

### Utility Agents

Glue and infrastructure agents — they do not directly produce output but make the rest of the canvas easier to wire.

| Agent | Description |
|-------|-------------|
| **Parametrizer** | The interconnection engine. Reads structured output from an upstream "source agent" (one of: Apirer, Gitter, Kuberneter, Crawler, Summarizer, Prompter, FlowCreator, File-Interpreter, Image-Interpreter, File-Extractor, Kyber-KeyGen, Kyber-Cipher, Kyber-DeCipher, Gatewayer, Gateway-Relayer, Googler, ACPXer) and writes selected fields into a downstream "target agent's" `config.yaml` via a visual interconnection scheme. When multiple output elements exist (e.g. Crawler returns 5 URLs), it iterates: fill config, start target, wait, repeat. The agent that turns a static canvas into a real data pipeline. |
| **FlowBacker** | A passive backup utility. When triggered (typically by Ender), it copies the entire deployed session pool directory to a configured `target_directory`, overwriting any previous backup of the same session, then launches its `target_agents` (Cleaners). The "save before you wipe" step in any flow that ends with cleanup. |
| **FlowCreator** | The AI flow designer. Reads `agentic_skill.md` (Tlamatini's library of canonical flow patterns) and asks an Ollama model to generate a complete `.flw` workflow JSON from a natural-language description. Use it to bootstrap a flow without dragging-and-wiring by hand: describe what you want, FlowCreator emits a flow you can then refine on the canvas. |
| **Gatewayer** | The inbound gateway. A long-running agent that listens for external events on either an HTTP webhook (default `127.0.0.1:8787`) or a folder-drop watcher, normalizes each event into a canonical envelope, persists it to disk, and dispatches downstream agents serially. Supports bearer-token, HMAC, and IP-allowlist authentication, plus dedup, persistence, and crash recovery. The agent that lets the outside world *call into* a flow — webhooks, IoT pushes, SaaS callbacks, scheduled batch drops, all welcome. |
| **Gateway Relayer** | A deterministic, no-LLM relay that bridges third-party webhook formats (e.g. GitHub's `X-Hub-Signature-256`) into Gatewayer's expected `timestamp + body` HMAC scheme. Validates the upstream provider's signature, transforms the payload, signs it for Gatewayer, and forwards it. Use it whenever you want a flow to receive native GitHub / GitLab / Stripe / Shopify webhooks without patching Gatewayer itself. |
| **NodeManager** | A long-running infrastructure registry. Maintains a live inventory of local and remote nodes, probes each one's health (ping, TCP, SSH, WinRM, HTTP), classifies state (`ONLINE` / `OFFLINE` / `DEGRADED` / `UNKNOWN`), detects capability changes, persists state across restarts, exports filtered manifests on demand, and triggers `target_agents` on configured node events. The control-tower agent for fleets of machines. |

### Cryptography Agents

Post-quantum cryptography primitives, deterministic and short-running.

| Agent | Description |
|-------|-------------|
| **Kyber-KeyGen** | Generates a CRYSTALS-Kyber post-quantum public/private key pair in base64. Three variants are supported: `kyber-512`, `kyber-768` (default), and `kyber-1024`. Output is logged in a structured block so a downstream Parametrizer can route the keys directly into Kyber-Cipher and Kyber-DeCipher. |
| **Kyber-Cipher** | Encrypts a plaintext buffer with a CRYSTALS-Kyber public key. Internally uses Kyber **encapsulation** to derive a shared secret and AES-256-CTR to encrypt the buffer; the agent logs the encapsulation, IV, and cipher text in base64, all wired so they can be piped into Kyber-DeCipher by Parametrizer. |
| **Kyber-DeCipher** | The inverse of Kyber-Cipher. Decrypts a base64 cipher text using the matching CRYSTALS-Kyber **private key** plus the encapsulation and IV that Kyber-Cipher emitted. Emits the recovered plaintext into its log, ready for File-Creator or another consumer. |

### Multi-Channel Bridges

Long-running listeners that bridge external messaging platforms into the full Multi-Turn Tlamatini chat (with tools, context, ACPX, and Exec Report).

| Agent | Description |
|-------|-------------|
| **TeleTlamatini** | A long-running Telegram bot that bridges authorized Telegram users into the **full Multi-Turn + Exec Report Tlamatini chat**. Each user message is treated as a chat turn; the answer (including tool calls, ACPX runs, and Exec Report tables) is rendered back into Telegram. Authorization is by Telegram user ID. The "use Tlamatini from your phone, while away from the keyboard" agent. |
| **WhatsTlamatini** | The WhatsApp twin of TeleTlamatini, built on the **Meta WhatsApp Cloud API**. Inbound messages arrive via a stdlib webhook listener; outbound replies are POSTed to the Graph API. Same Multi-Turn + Exec Report bridging, just over WhatsApp instead of Telegram. |

### External Coding-Agent Driver

The visual canvas counterpart of the 12 LLM-facing `acp_*` ACPX tools.

| Agent | Description |
|-------|-------------|
| **ACPXer** | Drives one external coding-agent CLI session per run — Claude Code, Codex, Cursor, Gemini, Qwen, the Tlamatini self-host, Kiro, Kimi, iFlow, Kilocode, OpenCode, Pi, Droid, or Copilot. The agent spawns the chosen `agent_id`, sends its `task`, waits for the child to settle (using a transport-aware drain rule), captures the assistant's last reply, and then kills the child cleanly. Output is emitted as a Parametrizer-compatible `INI_SECTION_ACPXER` block (`agent_id`, `session_id`, `transport`, `settle`, `transcript_path`, `response_body`), so chains of ACPXer nodes connected through Parametrizers form a fully visual multi-CLI relay — three different LLMs arguing back and forth in an unattended pipeline. |

---

## Notes on Maintenance

- **Adding a new agent**: append a new row to the relevant table above using the same Markdown format. The parser only requires `^\|\s*\*\*<Name>\*\*\s*\|\s*<description>\s*\|`. Section headings (`### ...`) are ignored by the parser; they exist purely for human readers.
- **Renaming an agent**: change the `**Name**` cell — but remember the lookup is normalized to lowercase alphanumeric, so `**Kyber-KeyGen**`, `**KyberKeyGen**`, and `**kyber keygen**` all hit the same slot. As long as your normalization matches the DB display name, the tooltip stays connected.
- **Frozen builds**: `build.py` copies this file into `dist/manage/agents_descriptions.md` so it ends up in the install directory next to `Tlamatini.exe`. The view loader probes `agents_descriptions.md` first, then falls back to `README.md` (so older deployments that haven't updated yet still work).
